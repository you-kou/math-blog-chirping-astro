---
title: '函数极限的概念'
description: '深入浅出解析数学分析中的函数极限概念：从自变量趋于有限值、趋于无穷大到极限不存在（发散）的三种核心场景与严格定义。'
pubDate: 1996-01-01 02:01:01
tags: [极限]
categories: [专转本]
heroImage: ./函数极限收敛发散分类.png
heroImageAlt: '函数极限收敛与发散分类讲解图'
---

import { Image } from 'astro:assets';
import XFiniteImg from './自变量趋于有限值的极限.png';
import XInftyImg from './自变量趋于无穷大的极限.png';
import PieImg from './分段函数左右极限.png';
import SinImg from './衰减震荡函数的极限.png';

在数学分析中，讨论函数极限的概念，本质上是在回答同一个核心问题：**当自变量 $x$ 按照某种特定规则逼近某个目标时，函数值 $f(x)$ 是否会稳定在一个确定的数值 $L$ 上？**

## 自变量趋于有限值 ($x \to a$)

这种情况讨论的是**函数在某一点附近的局部行为**。

> 💡 **核心逻辑**：极限只关注目标点 $a$ 的去心邻域 $\mathring{U}(a, \delta)$，完全不关心 $f(x)$ 在点 $a$ 本身是否有定义或等于多少。

### 概念与直观理解

当自变量 $x$ 从两侧无限接近某一个确定实数 $a$ 时，函数值 $f(x)$ 无限接近某一个确定数值 $L$。

### 严格语言表达（$\epsilon - \delta$ 语言）

设函数 $f(x)$ 在点 $a$ 的某个去心邻域内有定义。如果对于任意给定的 $\epsilon > 0$，总存在 $\delta > 0$，使得当 $0 < |x - a| < \delta$ 时，都有：

$$|f(x) - L| < \epsilon$$

则称 $L$ 为 $x \to a$ 时 $f(x)$ 的极限，记作 $\lim_{x \to a} f(x) = L$。

### 关键点：单侧极限

在有限点处，自变量可以从左右两个方向靠近 $a$：

- **左极限**：$\lim_{x \to a^-} f(x) = L_1$（约束区间为 $a - \delta < x < a$）
- **右极限**：$\lim_{x \to a^+} f(x) = L_2$（约束区间为 $a < x < a + \delta$）

**双侧极限存在的充要条件**：左极限 $L_1$ 与右极限 $L_2$ **均存在且相等**（即 $L_1 = L_2 = L$）。

<figure class="my-6">
  <Image src={XFiniteImg} alt="自变量趋于有限值的极限几何示意图" widths={[400, 800, 1200]} />
  <figcaption>自变量趋于有限值时的 $\epsilon - \delta$ 示意图</figcaption>
</figure>

## 自变量趋于无穷大 ($x \to \infty$)

这种情况讨论的是**函数在远端（无穷远处）的趋势行为**（在几何上常表现为水平渐近线）。

### 概念与直观理解

当自变量 $x$ 的绝对值变得越来越大（在数轴上向极远处延伸）时，函数值 $f(x)$ 无限接近某一个确定数值 $L$。

### 严格语言表达（$\epsilon - X$ 语言）

设函数 $f(x)$ 在 $|x|$ 足够大时有定义。如果对于任意给定的 $\epsilon > 0$，总存在正数 $X > 0$，使得当 $|x| > X$ 时，都有：

$$|f(x) - L| < \epsilon$$

则称 $L$ 为 $x \to \infty$ 时 $f(x)$ 的极限，记作 $\lim_{x \to \infty} f(x) = L$。

<figure class="my-6">
  <Image src={XInftyImg} alt="自变量趋于无穷大的极限几何示意图" widths={[400, 800, 1200]} />
  <figcaption>自变量趋于无穷大时的 $\epsilon - X$ 示意图</figcaption>
</figure>

### 关键点：$+\infty$ 与 $-\infty$ 的区别

$x \to \infty$ 包含两个方向：

- **$x \to +\infty$**：自变量向数轴极右端延伸，只需满足 $x > X$
- **$x \to -\infty$**：自变量向数轴极左端延伸，只需满足 $x < -X$

**$\lim_{x \to \infty} f(x) = L$ 存在的充要条件**：$x \to +\infty$ 和 $x \to -\infty$ 时的极限均存在且**完全相等**。
_例如：对于 $f(x) = e^x$，虽然 $\lim_{x \to -\infty} e^x = 0$，但由于当 $x \to +\infty$ 时函数发散，因此全局极限 $\lim_{x \to \infty} e^x$ 不存在。\_

## 极限不存在（发散，DNE）

如果函数值 $f(x)$ 在自变量逼近的过程中，**无法稳定地收紧在某一个唯一确定的实数 $L$ 上**，即称为极限不存在（Does Not Exist, DNE）。

在数学分析中，导致极限不存在的原因主要包含以下四种典型类型：

| 导致极限不存在的原因      | 几何/函数表现特点                        | 经典代表例子                                                                   |
| :------------------------ | :--------------------------------------- | :----------------------------------------------------------------------------- |
| **1. 趋于无穷大（无界）** | 函数值无限膨胀，无法收敛于任何有限实数   | $\lim_{x \to 0} \frac{1}{x^2} = +\infty$                                       |
| **2. 左右极限不相等**     | 函数在目标点附近存在“跳跃”间断点         | 符号函数 $\lim_{x \to 0} \operatorname{sgn}(x)$（左极限 $-1 \neq$ 右极限 $1$） |
| **3. 周期性或不衰减震荡** | 函数值在固定范围内持续交替跳跃，无法停下 | $\lim_{x \to \infty} \sin x$ 或 $\lim_{x \to 0} \sin\left(\frac{1}{x}\right)$  |
| **4. 域外逼近/无定义**    | 在逼近路径的去心邻域内，函数缺乏定义     | $f(x) = \sqrt{x}$ 当 $x \to -1$ 时                                             |

<figure class="my-6">
  <Image src={PieImg} alt="分段函数左右极限不相等导致的极限不存在" widths={[400, 800, 1200]} />
  <figcaption>左右极限不相等（跳跃间断点）导致极限不存在的经典场景</figcaption>
</figure>

### 重要延伸：震荡一定没有极限吗？

注意，导致极限不存在的是 **“无休止/不衰减的震荡”**。如果函数虽然也在频繁摆动，但**摆动幅度随着逼近目标而不断被收缩衰减**，那么极限依然存在。

<figure class="my-6">
  <Image
    src={SinImg}
    alt="衰减震荡函数的极限几何示意图"
    widths={[400, 800, 1200]}
  />
  <figcaption>$f(x) = x \sin\left(\frac{\pi}{x}\right)$ 当 $x \to 0$ 时的衰减震荡示例。虽然函数无限震荡，但振幅受到包络线 $y=\pm|x|$ 的挤压不断收缩，最终仍收敛于极限 $L=0$。</figcaption>
</figure>

## 总结：三者的逻辑关系

我们可以用一句话将上述场景统一起来：

> **“极限存在”要求自变量不论以何种方式（有限点逼近/无穷远延伸/左右侧逼近）接近目标，函数值 $f(x)$ 必须且只能“收敛并锁定”在唯一的有限实数 $L$ 上；一旦这个唯一的实数 $L$ 无法确定（无论是因为发散到无穷、左右分流还是持续摆动），就统一归为“极限不存在”。**
